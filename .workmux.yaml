# Dotfiles workmux config
panes:
  - command: n # fills window initially
    focus: true
  - command: <agent>
    split: vertical # right half
  - split: horizontal
    percentage: 30 # bottom 30% of right half (shell)

