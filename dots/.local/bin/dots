#!/bin/bash

cd ~/dotfiles

case $1 in 
  install)
    brew bundle
    ;;
  stow)
    stow --no-folding */ 
    ;;
  edit)
    nvim ./dots/.local/bin/dots
    ;;
  *)
    echo "pass a command: install, stow, edit"
esac

